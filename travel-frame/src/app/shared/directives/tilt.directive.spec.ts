import { TiltDirective } from './tilt.directive';

describe('TiltDirective', () => {
  it('should create an instance', () => {
    const directive = new TiltDirective();
    expect(directive).toBeTruthy();
  });
});
