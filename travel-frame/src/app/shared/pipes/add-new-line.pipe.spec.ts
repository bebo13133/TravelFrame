import { AddNewLinePipe } from './add-new-line.pipe';

describe('AddNewLinePipe', () => {
  it('create an instance', () => {
    const pipe = new AddNewLinePipe();
    expect(pipe).toBeTruthy();
  });
});
